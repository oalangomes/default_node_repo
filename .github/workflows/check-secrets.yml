name: 🔐 Verificar Segredos Necessários

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  check-secrets:
    runs-on: ubuntu-latest
    steps:
      - name: Verificar segredos obrigatórios
        run: |
          missing=0
          if [ -z "${{ secrets.CODECOV_TOKEN }}" ]; then echo "::error::O segredo CODECOV_TOKEN não está definido"; missing=1; fi
          if [ -z "${{ secrets.GITHUB_TOKEN }}" ]; then echo "::error::O segredo GITHUB_TOKEN não está definido"; missing=1; fi
          # Adicione outras chaves aqui se necessário
          if [ "$missing" -ne 0 ]; then exit 1; fi
          echo "✅ Todos os segredos obrigatórios estão presentes."
